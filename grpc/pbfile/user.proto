// 指定当前的proto语法的版本，有2和3
syntax = "proto3";
// option go_package = "path;name"; ath表示生成的go文件的存放地址，会自动生成目录
// name 表示生成的go文件所属的包名
option go_package = "../service";
// 指定等会文件生出来的package

// 消息 传输的对象
message User{
  string username = 1;
  int32 age = 2;
}